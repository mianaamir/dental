<div class="external">

<%= form_for(@tbl_form) do |f| %>
  <% if @tbl_form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tbl_form.errors.count, "error") %> prohibited this tbl_form from being saved:</h2>

      <ul>
      <% @tbl_form.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label :FormTitle %><br>
    <%= f.text_field :FormTitle %>
  </div>
  <div class="field">
    <%= f.label :FormDescription %><br>
    <%= f.text_area :FormDescription %>
  </div>
  <div class="field">
    <%= f.label :Type %><br>
    <%= f.text_field :Type %>
  </div>
  <div class="field">
    <%= f.label :Status %><br>
    <%= f.check_box :Status %>
  </div>
  <div class="actions">
    <%= f.submit "Submit", :class => "btn_submit" %>
  </div>
<% end %>
</div>