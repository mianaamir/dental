<%= form_for(@tbl_login) do |f| %>
  <% if @tbl_login.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tbl_login.errors.count, "error") %> prohibited this tbl_login from being saved:</h2>

      <ul>
      <% @tbl_login.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :UserName %><br>
    <%= f.text_field :UserName %>
  </div>
  <div class="field">
    <%= f.label :Password %><br>
    <%= f.password_field :Password %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
